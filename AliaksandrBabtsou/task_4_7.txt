import mod_c
import mod_b


print(mod_c.x)

    """
    Результат <5>
    из модуля  <mod_c> извлекаем значение <х> равное <5>
    из модуля  <mod_b> извлекаем значение <mod_c.x> равное <5>
    итого получается 5
    """

# Try to change x to a list [1,2,3]
import mod_c
import mod_b


print(mod_c.x)

    """
    Результат <5>
    из модуля  <mod_c> извлекаем значение <х> равное <[1,2,3]>
    из модуля  <mod_b> извлекаем значение <mod_c.x> равное <5>
    итого получается 5
    """


from mod_c import *
from mod_b import *


print(mod_c.x)

    """
    Результат <5>
    из модуля  <mod_c> извлекаем значение <х> равное <[1,2,3]>
    из модуля  <mod_b> извлекаем значение <mod_c.x> равное <5>
    итого получается 5    """
